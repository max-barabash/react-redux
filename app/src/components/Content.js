import React from 'react';

const Content = ({content}) => {
    return (
        <div id = 'content-container'>
            <p>{content}</p>
        </div>
    )
};

export default Content;